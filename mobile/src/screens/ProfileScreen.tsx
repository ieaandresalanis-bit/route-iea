/**
 * ProfileScreen — User info and settings.
 *
 * Shows:
 * - User name, email, role
 * - Assigned vehicle (if any)
 * - Trip history summary
 * - Logout button
 *
 * TODO: GET /api/auth/profile for user data
 * TODO: Show assigned vehicle info
 * TODO: Logout -> clearToken() -> navigate to Login
 */

import React from 'react';

export default function ProfileScreen() {
  return null; // Placeholder
}
